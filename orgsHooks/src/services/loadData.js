import header_mock from '../mocks/header_mock.js';
import producers from '../mocks/produtores.js';

export const loadProducers = () => {
    return producers;
}

export const loadHeader = () => {
    return header_mock;
}